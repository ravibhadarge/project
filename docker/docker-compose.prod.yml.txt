services:
  app:
    build: .
    env_file: .env
    depends_on: [mongo]
    networks: [web]

  mongo:
    image: mongo:6
    volumes:
      - mongo:/data/db

  nginx:
    image: nginx
    volumes:
      - ./nginx/letsencrypt.conf:/etc/nginx/conf.d/default.conf
      - ./certs:/etc/letsencrypt
    ports:
      - "80:80"
      - "443:443"
    networks: [web]

volumes:
  mongo:

networks:
  web: